if theme.friends_vue.enable
  .fcircle_page
    .author-content.author-content-item.fcirclePage.single(style = theme.essay.enable ? `background: url(${theme.friends_vue.top_background}) left 28% / cover no-repeat !important;` : "")
      .card-content
        .author-content-item-tips 友链
        span.author-content-item-title 最新文章订阅
        .content-bottom
          .tips 使用 友链朋友圈 订阅友链最新文章
        .banner-button-group
          a.banner-button(onclick='pjax.loadUrl("/about")')
            i.fas.fa-circle-right(style='font-size: 1.5rem')
            span.banner-button-text 关于我
    .title-h2-a
      .title-h2-a-left
        h2(style='padding-top:0;margin:.6rem 0 .6rem') 🎣 钓鱼
        a.random-post-start(href='javascript:fetchRandomPost();')
          i.fa-solid.fa-arrow-rotate-right
      .title-h2-a-right
        a.random-post-all(href='/link/') 全部友链
    #random-post
    #hexo-circle-of-friends-root

  if (theme.friends_vue.apiurl)
    include ../anzhiyu/random-friends-post-js.pug
  script(defer data-pjax src=url_for(theme.friends_vue.vue_js))
